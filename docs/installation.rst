Installation
============

Create a clean Python environment, install a PyTorch build compatible with the local CUDA runtime, and install SpaRMA from the repository root.

.. code-block:: bash

   conda create -n spaarma python=3.10
   conda activate spaarma
   pip install -e .
